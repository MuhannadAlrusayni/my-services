apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: faris-protoflio
  namespace: faris-protoflio
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    kubernetes.io/ingress.class: "traefik"
spec:
  rules:
    - host: "faris-protoflio.malrusayni.com"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: faris-protoflio-service
                port:
                  number: 80
  tls:
    - hosts:
        - "faris-protoflio.malrusayni.com"
      secretName: faris-protoflio-tls
