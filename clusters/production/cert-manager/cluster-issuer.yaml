apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: letsencrypt-prod
spec:
  acme:
    # The ACME server URL for Let’s Encrypt production
    server: https://acme-v02.api.letsencrypt.org/directory
    email: muhannad.alrusayni@gmail.com
    privateKeySecretRef:
      name: letsencrypt-prod
    solvers:
      - http01:
          ingress:
            class: traefik # <-- Replace with your ingress class if different
